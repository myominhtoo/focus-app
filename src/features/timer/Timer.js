import style from "../../styles"
import { View , Text } from "react-native"

export default function Timer(){
    return (
        <View style={style.container} >
            <Text style={style.title}>Hello I am timer</Text>
        </View>
    )
}